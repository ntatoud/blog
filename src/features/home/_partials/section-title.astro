---
import { cn } from '@/lib/utils';

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<h2
  class={cn(
    'font-head text-xl font-extrabold tracking-tight lg:text-3xl',
    className
  )}
>
  <slot />
</h2>
